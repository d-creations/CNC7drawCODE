version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - '8000:8000'
    volumes:
      - ./:/workspace:cached
    working_dir: /workspace/backend
    networks:
      - ncnet

networks:
  ncnet:
    driver: bridge
